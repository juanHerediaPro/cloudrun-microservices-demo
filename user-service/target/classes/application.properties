spring.application.name=user-service
server.port=8080

# === CONFIGURACIÓN JDBC ESTÁNDAR (Valores Directos - Sin Variables de Entorno) ===

# URL: Apunta a 127.0.0.1 (localhost), que Cloud Run intercepta automáticamente.
# Base de datos: microservicedemo (valor hardcodeado).
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/microservicedemo?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false

# Usuario: JuanDavid (ya confirmamos que este usuario existe en MySQL).
spring.datasource.username=JuanDavid

# Contraseña: Reemplaza con el valor exacto de tu secreto DB_PASSWORD.
# (Hemos confirmado que el usuario DB_USER funciona con su contraseña en MySQL).
spring.datasource.password=YO,rh>7hN{0^a08i 

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================================================
# JPA Configuration (Sin Cambios)
# =========================================================
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true